/** Gränser för det som skrivs in i riket. API:et och formulären läser samma värden. */
export const NAME_MAX = 40;
export const NOTE_MAX = 280;

/** Ett pingisrike, inte en folkräkning. Taket håller också listorna som hämtar alla spelare små. */
export const PLAYERS_MAX = 100;
